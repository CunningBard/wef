# Binary transform blobs

Fixture JSON refers to these files by name. Blob bytes never appear inline in
fixture JSON. `black.ppm` is a deterministic 1×1 image used for decode/encode
and malformed-input boundary checks.
